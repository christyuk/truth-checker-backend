export const analyzeTruth = (text) => {
  // Demo logic (replace with AI later)
  if (text.toLowerCase().includes("earth")) {
    return "Likely TRUE (demo logic)";
  }

  return "Cannot verify (demo logic)";
};
